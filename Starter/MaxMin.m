% Reminder: [Maximum, Minimum] is NOT a vector
function [Maximum, Minimum] = MaxMin(z)
%Calculates the maximum and the minimum of input vector
%Input:
%   z: a vector
%Output:
%   Maximum: max(z)
%   Minimum: min(z)
Maximum = max(z);
Minimum = min(z);
end

